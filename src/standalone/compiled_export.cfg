# Stock-compatible COMPILE export baseline image.
# Raw payload starts at $0801; the build prepends the PRG load address.

MEMORY {
    RAM: start = $0801, size = $C7FF, file = %O, fill = no;
}

SEGMENTS {
    CODE: load = RAM, type = ro;
}
